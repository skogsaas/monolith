<UserControl
    x:Class="Obelisk.Tiles.Slider"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Obelisk"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="uc"
    d:DesignHeight="100"
    d:DesignWidth="200">

    <StackPanel Orientation="Vertical" Margin="5">
        <StackPanel.Background>
            <SolidColorBrush Color="Beige" Opacity="0.25" />
        </StackPanel.Background>
        
        <TextBlock Text="{Binding Path=Identifier, ElementName=uc}" />
        <Slider Orientation="Horizontal" Value="{Binding State, ElementName=uc, Mode=TwoWay}" HorizontalAlignment="Stretch" Width="200" />
    </StackPanel>
</UserControl>
