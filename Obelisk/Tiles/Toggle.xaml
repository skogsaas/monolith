<UserControl
    x:Class="Obelisk.Tiles.Toggle"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Obelisk"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="uc"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="200">

    <StackPanel Orientation="Vertical" Margin="5">
        <StackPanel.Background>
            <SolidColorBrush Color="Beige" Opacity="0.25" />
        </StackPanel.Background>
        
        <TextBlock Text="{Binding Path=Identifier, ElementName=uc}" />
        <ToggleSwitch IsOn="{Binding Path=State, ElementName=uc, Mode=TwoWay}" />
    </StackPanel>
</UserControl>
